# Build output
/router
/bin/

# Local snapshot cache (when run with --cache pointing into the repo)
/snapshot.json

# Secrets — never commit API keys
artificial-analysis-api-key
aa-api-key
openrouter-api-key

# Editor / OS cruft
*.swp
.DS_Store
